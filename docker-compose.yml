version: '3.8'

services:
  # PostgreSQL 16 with pgvector
  postgres:
    image: pgvector/pgvector:pg16
    container_name: aidbid-postgres
    environment:
      POSTGRES_DB: aidbid
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aidbid-network

  # Redis
  redis:
    image: redis:7-alpine
    container_name: aidbid-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aidbid-network

  # Chroma Vector Database
  chroma:
    image: chromadb/chroma:0.4.22
    container_name: aidbid-chroma
    ports:
      - "${CHROMA_PORT:-8000}:8000"
    volumes:
      - chroma_data:/chroma/chroma_data
    environment:
      - IS_PERSISTENT=TRUE
    networks:
      - aidbid-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Gateway Service
  gateway:
    build:
      context: .
      dockerfile: ai-bid-gateway/Dockerfile
    container_name: aidbid-gateway
    ports:
      - "${GATEWAY_PORT:-8080}:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES:-docker}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - aidbid-network

  # User Service
  user-service:
    build:
      context: .
      dockerfile: ai-bid-user/Dockerfile
    container_name: aidbid-user
    ports:
      - "${USER_PORT:-8081}:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES:-docker}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=aidbid
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aidbid-network

  # Project Service
  project-service:
    build:
      context: .
      dockerfile: ai-bid-project/Dockerfile
    container_name: aidbid-project
    ports:
      - "${PROJECT_PORT:-8082}:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES:-docker}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=aidbid
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - aidbid-network

  # Material Service
  material-service:
    build:
      context: .
      dockerfile: ai-bid-material/Dockerfile
    container_name: aidbid-material
    ports:
      - "${MATERIAL_PORT:-8083}:8083"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES:-docker}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=aidbid
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - aidbid-network

  # Document Service
  document-service:
    build:
      context: .
      dockerfile: ai-bid-document/Dockerfile
    container_name: aidbid-document
    ports:
      - "${DOCUMENT_PORT:-8084}:8084"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES:-docker}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=aidbid
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - aidbid-network

  # Knowledge Service (RAG Knowledge Base)
  knowledge:
    build:
      context: .
      dockerfile: ai-bid-knowledge/Dockerfile
    container_name: aidbid-knowledge
    ports:
      - "${KNOWLEDGE_PORT:-8086}:8086"
    environment:
      - LLM_PROVIDER=${LLM_PROVIDER:-minimax}
      - MINIMAX_API_KEY=${MINIMAX_API_KEY:-}
      - MINIMAX_BASE_URL=${MINIMAX_BASE_URL:-https://api.minimax.chat/v1}
      - MINIMAX_MODEL=${MINIMAX_MODEL:-abab6-chat}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - DEEPSEEK_BASE_URL=${DEEPSEEK_BASE_URL:-https://api.deepseek.com/v1}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL:-deepseek-chat}
      - QWEN_API_KEY=${QWEN_API_KEY:-}
      - QWEN_BASE_URL=${QWEN_BASE_URL:-https://dashscope.aliyuncs.com/api/v1}
      - QWEN_MODEL=${QWEN_MODEL:-qwen-turbo}
      - CHROMA_HOST=chroma
      - CHROMA_PORT=8000
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-shibing624/text2vec-base-chinese}
      - EMBEDDING_DEVICE=${EMBEDDING_DEVICE:-cpu}
    depends_on:
      chroma:
        condition: service_healthy
    networks:
      - aidbid-network

  # AI Service (Bid Generation & Processing)
  ai:
    build:
      context: .
      dockerfile: ai-bid-ai/Dockerfile
    container_name: aidbid-ai
    ports:
      - "${AI_PORT:-8087}:8087"
    environment:
      - LLM_PROVIDER=${LLM_PROVIDER:-minimax}
      - MINIMAX_API_KEY=${MINIMAX_API_KEY:-}
      - MINIMAX_BASE_URL=${MINIMAX_BASE_URL:-https://api.minimax.chat/v1}
      - MINIMAX_MODEL=${MINIMAX_MODEL:-abab6-chat}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - DEEPSEEK_BASE_URL=${DEEPSEEK_BASE_URL:-https://api.deepseek.com/v1}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL:-deepseek-chat}
      - QWEN_API_KEY=${QWEN_API_KEY:-}
      - QWEN_BASE_URL=${QWEN_BASE_URL:-https://dashscope.aliyuncs.com/api/v1}
      - QWEN_MODEL=${QWEN_MODEL:-qwen-turbo}
      - KNOWLEDGE_SERVICE_URL=http://knowledge:8086
      - MAX_TOKENS=${MAX_TOKENS:-8192}
      - TEMPERATURE=${TEMPERATURE:-0.7}
    depends_on:
      knowledge:
        condition: service_started
    networks:
      - aidbid-network

networks:
  aidbid-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  chroma_data: